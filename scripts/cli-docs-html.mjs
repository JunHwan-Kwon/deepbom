// The hosted reference consumes the same executable help and capability object
// as the Markdown reference. Keep hand-authored workflows in the handbook.
export function renderCliReferenceHtml(help, capabilities, { agentTextSupported = true } = {}) {
  const notes = {
    audit: ["audit", "Inspect one supported artifact or package."],
    gguf: ["inspect", "Inspect GGUF metadata and tensor evidence."],
    batch: ["automation", "Audit manifest-bound files in sequence."],
    verify: ["compare", "Compare an artifact with an interface declaration or CycloneDX BOM."],
    "contract capture": ["compare", "Capture an artifact-derived external-interface baseline."],
    diff: ["compare", "Compare two artifacts of the same supported format."],
    explore: ["targets", "Evaluate a declared TFLite redesign request."],
    graph: ["visualize", "Export a deterministic graph view."],
    "model-summary": ["inspect", "Show Model IR operation, block, or storage rows."],
    visualize: ["visualize", "Export document-oriented Model IR pages and captions."],
    placement: ["targets", "Compare independent static or imported placement evidence."],
    "accelerator collect nvidia": ["targets", "Collect a local NVIDIA host profile; does not execute the model."],
    "explain-rule": ["inspect", "Explain a finding rule or list available rules."],
    "self-test": ["start", "Check the installed analyzer with its bundled fixture."],
    capabilities: ["agents", "Return the machine-readable CLI or agent contract."],
    integrate: ["agents", "Preview or apply managed local-agent skill changes."],
  };
  const rows = capabilities.commands.map(command => {
    const note = notes[command.name];
    const link = note ? `../#${note[0]}` : "#help";
    return `      <tr data-command-row><th scope="row"><a href="${link}"><code>${escapeHtml(command.name)}</code></a></th><td>${escapeHtml(note?.[1] || "See the executable help below.")}</td><td>${command.outputs.map(output => `<code>${escapeHtml(output)}</code>`).join("<br />")}</td></tr>`;
  });
  // MCP is a transport, not an artifact command, so capabilities.commands does
  // not assign it an evidence-output schema. Preserve that distinction here.
  rows.push('      <tr data-command-row><th scope="row"><a href="../#agents"><code>mcp</code></a></th><td>Serve four local assistant tools over standard input/output.</td><td>MCP transport; no standalone evidence document</td></tr>');
  const version = escapeHtml(capabilities.cli_version);
  const helpCorrection = agentTextSupported ? "" : `<div class="book-note"><p><strong>Verified behavior in ${version}:</strong> the help advertises <code>capabilities --format agent-text</code>, but the command currently rejects it. Use <code>capabilities --format agent-json</code>. The help below is preserved verbatim; the handbook uses the working invocation.</p></div>`;
  return `<!doctype html>
<!-- Generated by scripts/generate-cli-docs.mjs from the executable. Do not edit. -->
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>CLI Command Reference | DEEPBOM Guides</title>
  <meta name="description" content="The DEEPBOM ${version} CLI command inventory, output contracts, and complete executable help. Search commands and copy options from the versioned reference." />
  <link rel="canonical" href="https://deepbom.org/guides/cli/reference/" />
  <meta property="og:title" content="DEEPBOM CLI Command Reference" />
  <meta property="og:description" content="Command inventory and complete help, generated from the executable CLI." />
  <script type="application/ld+json">{"@context":"https://schema.org","@type":"TechArticle","headline":"DEEPBOM CLI Command Reference","url":"https://deepbom.org/guides/cli/reference/","version":"${version}","about":{"@id":"https://deepbom.org/#app"}}</script>
  <link rel="icon" href="/chatgpt/deepbom-app-icon.svg" type="image/svg+xml" />
  <link rel="stylesheet" href="../../../evaluate/evaluation.css" />
  <link rel="stylesheet" href="../book.css" />
  <script type="module" src="../book.js"></script>
  <script type="application/ld+json">{"@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"Guides","item":"https://deepbom.org/guides/"},{"@type":"ListItem","position":2,"name":"CLI Handbook","item":"https://deepbom.org/guides/cli/"},{"@type":"ListItem","position":3,"name":"Command reference","item":"https://deepbom.org/guides/cli/reference/"}]}</script>
</head>
<body class="cli-book">
  <a class="book-skip" href="#main">Skip to reference</a>
  <header class="brief-header"><a class="brand" href="/">DEEPBOM</a><nav class="brief-nav" aria-label="Main navigation"><a href="/">Workspace</a><a href="/get-started/">Get started</a><a href="/guides/" aria-current="location">Guides</a><a href="/evaluate/">Evaluate</a></nav></header>
  <div class="book-layout">
    <aside class="book-sidebar"><p class="eyebrow">CLI ${version}</p><nav aria-label="Reference sections"><a href="#commands">Command index</a><a href="#help">Complete help &amp; options</a><a href="#machine">Machine discovery</a><a href="../">← Practical handbook</a><a href="/get-started/#cli">Installation methods</a></nav></aside>
    <main class="book-main" id="main">
      <nav class="breadcrumbs" aria-label="Breadcrumb"><ol><li><a href="/guides/">Guides</a></li><li><a href="/guides/cli/">CLI Handbook</a></li><li><span aria-current="page">Command reference</span></li></ol></nav>
      <p class="eyebrow">CLI reference / ${version}</p>
      <h1>Command reference</h1>
      <p class="lede">The command inventory and help below are generated from DEEPBOM ${version}. For step-by-step examples and interpretation, read the <a href="../">CLI Handbook</a>.</p>
      <div class="book-actions"><a href="./help.txt" download="deepbom-${version}-help.txt">Download plain-text help</a><button type="button" data-print hidden>Print / Save as PDF</button></div>
      ${helpCorrection}
      <section id="commands"><p class="eyebrow">Find a command</p><h2>Command index</h2>
        <div class="command-search" hidden><label for="command-search">Search commands or output formats</label><input id="command-search" type="search" placeholder="Try audit, CycloneDX, SVG, or MCP" aria-controls="command-table" /><p id="command-count" role="status">${rows.length} commands</p></div>
        <table class="book-table" id="command-table"><caption>Select a command for its handbook examples. Output contracts are declared by CLI capabilities.</caption><thead><tr><th scope="col">Command</th><th scope="col">Purpose</th><th scope="col">Declared outputs</th></tr></thead><tbody>
${rows.join("\n")}
        </tbody></table><p id="command-empty" hidden>No matching commands. Clear the search or inspect the complete help below.</p>
      </section>
      <section id="help"><p class="eyebrow">Executable reference</p><h2>Complete help &amp; options</h2><p>This is the normalized output of <code>deepbom --help</code>. Options are command-specific; the CLI rejects unsupported combinations. Use your browser's Find command to locate an option.</p><div class="book-code"><pre><code>${escapeHtml(help.trimEnd())}</code></pre></div></section>
      <section id="machine"><p class="eyebrow">For scripts and agents</p><h2>Discover the installed contract.</h2><div class="book-code"><pre><code>deepbom --version
deepbom capabilities --compact
deepbom capabilities --format agent-json</code></pre></div><p>Automations should read <code>${escapeHtml(capabilities.schema)}</code> instead of parsing human help. Runtime placement, latency, task accuracy, and regulatory conclusions require separate evidence.</p></section>
      <nav class="book-footer" aria-label="Continue reading"><a href="../">← CLI Handbook</a><a href="/guides/">All guides →</a></nav>
    </main>
  </div>
  <p id="book-status" class="book-status" role="status" aria-live="polite"></p>
  <footer class="brief-footer">Generated from the same executable and capabilities as the repository CLI reference. Version ${version}.</footer>
</body>
</html>
`;
}

function escapeHtml(value) {
  return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
}
